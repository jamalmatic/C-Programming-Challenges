#include <stdio.h>

int main()
{
    float principal, rate, time;

    // Ask the user to enter principal, rate, and time
    printf("Enter Principal, Rate, and Time (either space-separated or one per line):\n");

    // Read the three values
    scanf("%f %f %f", &principal, &rate, &time);
    // Note: This also works if the user presses Enter after each value

    // TODO: Declare a variable to store the interest
    // Example: float interest;

    // TODO: Calculate simple interest using the formula
    // Interest = (Principal × Rate × Time) / 100

    // TODO: Print the interest, with no decimal places, in the format:
    // Interest = <value>

    

    return 0;
}