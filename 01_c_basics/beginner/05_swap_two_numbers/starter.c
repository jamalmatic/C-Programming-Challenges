#include <stdio.h>

int main()
{
    int a, b;

    // Ask the user to enter two integers
    printf("Enter two integers: ");

    // Read the two numbers
    scanf("%d %d", &a, &b);

    // TODO: Swap the values of a and b using a temporary variable
        
    
    // Print the swapped values in the format:
    // a = <value>, b = <value>
    printf("a = %d, b = %d\n", a, b);
    

    return 0;
}