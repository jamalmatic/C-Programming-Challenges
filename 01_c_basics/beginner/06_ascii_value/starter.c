#include <stdio.h>

int main()
{
    char ch;

    // Ask the user to enter a character
    printf("Enter a character: ");

    // Read the character
    scanf("%c", &ch);

    // TODO: Print the ASCII value of the character
    // Hint: Use %d format in printf

    

    return 0;
}